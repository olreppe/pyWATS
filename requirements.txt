# PyWATS Core Runtime Dependencies
# 
# NOTE: This file is for reference only. Install via PyPI for proper dependency management:
#   pip install pywats-api
#
# For optional dependencies: pip install pywats-api[client] or pywats-api[dev]

# Core dependencies
httpx[http2]>=0.24.0  # HTTP client for REST API with HTTP/2 support
pydantic>=1.10.0  # Data validation and serialization
python-dateutil>=2.8.0  # Date parsing utilities
attrs>=22.0.0  # Data classes and attributes for generated client

# Optional dependencies for enhanced functionality
requests>=2.28.0  # Alternative HTTP client (if needed)
urllib3>=1.26.0  # URL handling

# Async support (for AsyncClientService)
aiofiles>=23.0.0  # Async file I/O
qasync>=0.27.0  # Qt + asyncio integration

# Development dependencies
pytest>=7.0.0  # Testing framework
pytest-asyncio>=0.21.0  # Async testing support
pytest-mock>=3.10.0  # Mocking for tests
black>=22.0.0  # Code formatting
isort>=5.10.0  # Import sorting
mypy>=1.0.0  # Type checking
flake8>=5.0.0  # Linting

# Documentation dependencies
sphinx>=5.0.0  # Documentation generation
sphinx-rtd-theme>=1.2.0  # Read the Docs theme