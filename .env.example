# Environment variables for Docker Compose
# Copy this file to .env and fill in your values

# ============================================================================
# WATS API Configuration (REQUIRED)
# ============================================================================

# WATS Server URL (e.g., https://wats.yourcompany.com or http://localhost:8080)
WATS_BASE_URL=https://wats.example.com

# Authentication token (Base64-encoded username:password)
# Generate with: echo -n "username:password" | base64
WATS_TOKEN=

# ============================================================================
# Client Configuration
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
PYWATS_LOG_LEVEL=INFO

# ============================================================================
# pyWATS Client Debugging (OPTIONAL)
# ============================================================================

# Server URL and token for client debugging
# Only used if client config file has empty values
# DO NOT commit .env with real tokens - it's gitignored
PYWATS_SERVER_URL=https://python.wats.com
PYWATS_API_TOKEN=your_token_here

# ============================================================================
# Directory Paths
# ============================================================================

# Watch directory - incoming test data files
WATCH_DIR=./watch

# Output directory - converted WATS reports
OUTPUT_DIR=./output

# Archive directory - processed files
ARCHIVE_DIR=./archive

# ============================================================================
# MCP Server Configuration
# ============================================================================

# Port for MCP server (AI assistant integration)
MCP_PORT=3000

# ============================================================================
# Notes
# ============================================================================
# 
# 1. DO NOT commit .env to version control (it's in .gitignore)
# 2. Create directories before running: mkdir -p watch output archive config
# 3. Place client config in ./config/client_config.json
# 4. Start services with: docker-compose up -d client
# 5. View logs with: docker-compose logs -f client
# 6. Stop services with: docker-compose down
