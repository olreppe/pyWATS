╔═══════════════════════════════════════════════════════════════════════════╗
║                    PARALLEL BRANCH WORKFLOW CHEATSHEET                    ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────────────┐
│  QUESTION: Can agents work in background while I work locally?           │
│  ANSWER:   YES! ✓ Branches are independent and isolated.                 │
└───────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────┐
│  THE WORKFLOW                                                              │
└───────────────────────────────────────────────────────────────────────────┘

  YOU (Local)                     AGENT (Remote)
  ───────────                     ──────────────
  
  1. Create branch                1. Agent creates branch
     $ git checkout -b               copilot/task-name
       feature/my-work               
                                   2. Agent commits changes
  2. Make changes                    (happens on GitHub)
     $ git commit -m "..."           
                                   3. Agent opens PR #123
  3. Keep working                    (ready for review)
     $ git commit -m "..."
     $ git commit -m "..."        4. You review & merge PR
                                      (via GitHub UI)
  4. Optional: integrate
     $ git pull origin main       
     $ git rebase main            
                                   
  5. Open your PR                 
     $ git push origin             
       feature/my-work             

  RESULT: Both tasks complete in parallel! 🎉

┌───────────────────────────────────────────────────────────────────────────┐
│  KEY COMMANDS                                                              │
└───────────────────────────────────────────────────────────────────────────┘

  View all branches:
    $ git branch -a

  Check agent's work:
    $ git fetch origin
    $ git checkout copilot/task-name

  Return to your work:
    $ git checkout your-branch

  Integrate agent's merged changes:
    $ git checkout main
    $ git pull origin main
    $ git checkout your-branch
    $ git rebase main          (or: git merge main)

┌───────────────────────────────────────────────────────────────────────────┐
│  COMMON SCENARIOS                                                          │
└───────────────────────────────────────────────────────────────────────────┘

  Scenario 1: Independent files
    You:   Edit src/feature.py
    Agent: Edit docs/API.md
    Result: No conflicts, merge anytime ✓

  Scenario 2: Same file, different parts
    You:   Add function foo() to utils.py
    Agent: Add function bar() to utils.py
    Result: Git merges automatically ✓

  Scenario 3: Same file, same location
    You:   Change line 42 in config.py
    Agent: Change line 42 in config.py
    Result: Git asks you to resolve conflict
            (easy to fix during merge)

┌───────────────────────────────────────────────────────────────────────────┐
│  BRANCH VISUALIZATION                                                      │
└───────────────────────────────────────────────────────────────────────────┘

  Your Repository Structure:

    main (production)
     │
     ├───────────┬────────────┬───────────┐
     │           │            │           │
  feature/   copilot/    copilot/    copilot/
  your-work  docs        bugfix      refactor
     │           │            │           │
    (you)     (agent)     (agent)     (agent)

  Each branch is independent!
  Merge in any order, when ready.

┌───────────────────────────────────────────────────────────────────────────┐
│  BENEFITS                                                                  │
└───────────────────────────────────────────────────────────────────────────┘

  ✓ No waiting      - Work in parallel
  ✓ No blocking     - Independent progress
  ✓ Safe isolation  - Branches don't interfere
  ✓ Easy review     - PRs for each task
  ✓ Flexible merge  - Integrate when ready
  ✓ Clean history   - Proper Git workflow

┌───────────────────────────────────────────────────────────────────────────┐
│  STARTING AN AGENT TASK                                                    │
└───────────────────────────────────────────────────────────────────────────┘

  Via GitHub Issue:
    Comment: "@copilot Please refactor the auth module"

  Via GitHub Copilot Workspace:
    1. Open repository on GitHub
    2. Use Copilot interface
    3. Describe task
    4. Agent creates branch and works

  Via PR Comment:
    Comment: "@copilot Add type hints to all functions"

┌───────────────────────────────────────────────────────────────────────────┐
│  TROUBLESHOOTING                                                           │
└───────────────────────────────────────────────────────────────────────────┘

  "Can't see agent's branch"
    → git fetch origin
    → git branch -a

  "Uncommitted changes blocking checkout"
    → git stash
    → git checkout other-branch
    → git stash pop (when back)

  "Merge conflict occurred"
    → Open conflicted file
    → Look for <<<<<<< and >>>>>>>
    → Choose what to keep
    → git add file
    → git commit

  "Want to cancel agent's work"
    → Close the PR on GitHub
    → Your work unaffected

┌───────────────────────────────────────────────────────────────────────────┐
│  DOCUMENTATION                                                             │
└───────────────────────────────────────────────────────────────────────────┘

  📖  Complete Guide:     docs/WORKFLOW_PARALLEL_BRANCHES.md
  🚀  Quick Reference:    docs/QUICK_REFERENCE_PARALLEL_WORK.md  
  ❓  FAQ (25+ Q&A):      docs/FAQ_PARALLEL_WORKFLOWS.md
  📊  Visual Diagrams:    docs/WORKFLOW_DIAGRAM.md
  🎬  Example Script:     docs/examples/parallel_branch_workflow.sh

  Run example:
    $ bash docs/examples/parallel_branch_workflow.sh

┌───────────────────────────────────────────────────────────────────────────┐
│  SUMMARY                                                                   │
└───────────────────────────────────────────────────────────────────────────┘

  ✅  YES - Agents CAN work in background branches
  ✅  Your local work is NEVER disrupted
  ✅  Branches are ISOLATED and SAFE
  ✅  You CONTROL integration via PRs
  ✅  UNLIMITED parallel tasks supported
  ✅  Standard Git workflow - NOTHING SPECIAL NEEDED

  This is exactly what Git was designed for! 🎉

╔═══════════════════════════════════════════════════════════════════════════╗
║  Need more help? See docs/FAQ_PARALLEL_WORKFLOWS.md                       ║
╚═══════════════════════════════════════════════════════════════════════════╝
